import React from "react";
import Button from "./components/Button";
import "./styles.css";

const App = () => {
  return (
    <div>
      <Button isOn={false} onClick={() => {}} />
    </div>
  );
};

export default App;
